{
  "manifest_version": 3,
  "name": "ClipTown Draft Recovery",
  "version": "0.1.0",
  "description": "Consent-based, local draft recovery for selected sites while ClipTown encrypted sync is under development.",
  "permissions": ["activeTab", "scripting", "storage"],
  "optional_host_permissions": ["http://*/*", "https://*/*"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_title": "ClipTown capture settings",
    "default_popup": "popup.html"
  }
}
