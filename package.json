{
  "name": "cliptown-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check:syntax": "node --check background-policy.js && node --check background.js && node --check content.js && node --check policy.js && node --check popup.js",
    "check:manifest": "node scripts/validate-extension.mjs",
    "test": "node --test test/*.test.js",
    "test:browser": "playwright test",
    "check": "npm run check:syntax && npm run check:manifest && npm test",
    "lint:ores": "if [ -f .ores-lint/lint.sh ]; then sh .ores-lint/lint.sh; else echo 'ores-lint not installed'; fi",
    "prepublishOnly": "npm run lint:ores"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  },
  "oresLint": {
    "note": "Declared, not installed. ores-lint resolves these from a global install - see .ores-lint/README.md",
    "requires": {
      "eslint": ">=9",
      "typescript-eslint": ">=8 (optional; without it .ts files are skipped)"
    },
    "install": "npm i -g eslint typescript-eslint"
  }
}
